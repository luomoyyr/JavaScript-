window.onload = function () {
    var oUl = document.getElementById("list");
    var childNodesLen = oUl.childNodes.length;
    var childNodesLen = oUl.children.length;
    alert("childNodes的长度为：" + childNodesLen + "\n" + "children的长度为：" + childNodesLen);
}